<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Color x:Key="WindowBackgroundColor">#FFF1F1F2</Color>
  <Color x:Key="PrimaryTextColor">#FF111111</Color>
  <Color x:Key="SecondaryTextColor">#FF4B5563</Color>
  <Color x:Key="MutedTextColor">#FF6B7280</Color>
  <Color x:Key="AccentColor">#FFC62828</Color>
  <Color x:Key="AccentDarkColor">#FF8E1B1B</Color>
  <Color x:Key="AccentSoftColor">#FFFDECEC</Color>
  <Color x:Key="CardFillColor">#CCFFFFFF</Color>
  <Color x:Key="PanelFillColor">#D6F4F5F7</Color>
  <Color x:Key="SurfaceFillColor">#CCF6F6F7</Color>
  <Color x:Key="SubtleBorderColor">#33111111</Color>
  <Color x:Key="InputBorderColor">#66555555</Color>
  <Color x:Key="InputFillColor">#FFF8FAFC</Color>
  <Color x:Key="SuccessFillColor">#FFE8F5EE</Color>
  <Color x:Key="SuccessBorderColor">#FF8BC8A8</Color>
  <Color x:Key="WarningFillColor">#FFF7EAD9</Color>
  <Color x:Key="WarningBorderColor">#FFE0BE88</Color>

  <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}" />
  <SolidColorBrush x:Key="PrimaryTextBrush" Color="{StaticResource PrimaryTextColor}" />
  <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}" />
  <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedTextColor}" />
  <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
  <SolidColorBrush x:Key="AccentDarkBrush" Color="{StaticResource AccentDarkColor}" />
  <SolidColorBrush x:Key="AccentSoftBrush" Color="{StaticResource AccentSoftColor}" />
  <SolidColorBrush x:Key="CardFillBrush" Color="{StaticResource CardFillColor}" />
  <SolidColorBrush x:Key="PanelFillBrush" Color="{StaticResource PanelFillColor}" />
  <SolidColorBrush x:Key="SurfaceFillBrush" Color="{StaticResource SurfaceFillColor}" />
  <SolidColorBrush x:Key="SubtleBorderBrush" Color="{StaticResource SubtleBorderColor}" />
  <SolidColorBrush x:Key="InputBorderBrush" Color="{StaticResource InputBorderColor}" />
  <SolidColorBrush x:Key="InputFillBrush" Color="{StaticResource InputFillColor}" />
  <SolidColorBrush x:Key="SuccessFillBrush" Color="{StaticResource SuccessFillColor}" />
  <SolidColorBrush x:Key="SuccessBorderBrush" Color="{StaticResource SuccessBorderColor}" />
  <SolidColorBrush x:Key="WarningFillBrush" Color="{StaticResource WarningFillColor}" />
  <SolidColorBrush x:Key="WarningBorderBrush" Color="{StaticResource WarningBorderColor}" />

  <LinearGradientBrush x:Key="HeroGradientBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Offset="0" Color="#CCF1F1F2" />
    <GradientStop Offset="0.48" Color="#CCE5E7EB" />
    <GradientStop Offset="1" Color="#CCF6F6F7" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="AccentStripBrush" StartPoint="0,0" EndPoint="0,1">
    <GradientStop Offset="0" Color="#FFC62828" />
    <GradientStop Offset="1" Color="#FFEF4444" />
  </LinearGradientBrush>

  <Style TargetType="Window">
    <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
    <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
    <Setter Property="TextElement.Foreground" Value="{StaticResource PrimaryTextBrush}" />
  </Style>

  <Style x:Key="HeroCardStyle" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource CardFillBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="16" />
    <Setter Property="Padding" Value="16" />
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect BlurRadius="26" ShadowDepth="0" Color="#140F172A" Opacity="0.18" />
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ShellCardStyle" TargetType="Border" BasedOn="{StaticResource HeroCardStyle}">
    <Setter Property="Background" Value="{StaticResource SurfaceFillBrush}" />
    <Setter Property="CornerRadius" Value="14" />
    <Setter Property="Padding" Value="8" />
  </Style>

  <Style x:Key="PanelCardStyle" TargetType="Border" BasedOn="{StaticResource HeroCardStyle}">
    <Setter Property="Background" Value="{StaticResource CardFillBrush}" />
    <Setter Property="CornerRadius" Value="14" />
    <Setter Property="Padding" Value="16" />
  </Style>

  <Style x:Key="PrimaryButtonStyle" TargetType="Button">
    <Setter Property="Foreground" Value="White" />
    <Setter Property="Background" Value="{StaticResource AccentBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource AccentDarkBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="16,0" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="MinHeight" Value="40" />
    <Setter Property="MinWidth" Value="110" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="9">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{StaticResource AccentDarkBrush}" />
              <Setter Property="BorderBrush" Value="{StaticResource AccentDarkBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="#FF6E1111" />
              <Setter Property="BorderBrush" Value="#FF6E1111" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
    <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
    <Setter Property="Background" Value="{StaticResource InputFillBrush}" />
    <Setter Property="BorderBrush" Value="#FFD1D5DB" />
  </Style>

  <Style x:Key="PillButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
    <Setter Property="MinWidth" Value="100" />
    <Setter Property="Padding" Value="14,0" />
    <Setter Property="MinHeight" Value="40" />
  </Style>

  <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="#FF374151" />
    <Setter Property="Margin" Value="0,0,0,8" />
  </Style>

  <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
    <Setter Property="FontSize" Value="16" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
  </Style>

  <Style x:Key="SectionSubtitleStyle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
  </Style>

  <Style TargetType="TextBox">
    <Setter Property="Background" Value="{StaticResource InputFillBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="12,10" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
    <Setter Property="MinHeight" Value="44" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="8">
            <ScrollViewer x:Name="PART_ContentHost" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
